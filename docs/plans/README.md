# Execution plans

Plans are durable work state for coding agents and humans.

Use:

- `active/` for work currently in progress;
- `completed/` for finished plans.

A plan should be updated as implementation discovers new facts.

Use `0000-template.md` as a starting point.
