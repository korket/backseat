# Eval scenarios

Add scenario definitions here once the eval format is supported.

Keep scenarios declarative where practical.

Each scenario should state:

- setup;
- target;
- actions or agent goal;
- safety expectations;
- observable success criteria.

Safety expectations must reference `docs/runbooks/cua-agent-safety.md` and include cursor, focus, and delivery-route receipt fields.
